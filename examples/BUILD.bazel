load("//wasm:wasm_binary.bzl", "wasm_binary")

wasm_binary(
    name = "hello",
    srcs = ["hello.c"],
    out = ["hello.js", "hello.wasm"],
    target_compatible_with = [
        "@platforms//os:wasi",
        "@platforms//cpu:wasm64"
    ]
)
